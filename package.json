{
  "name": "wtp-mcp-rs-workspace",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "npm/base",
    "npm/platform/*"
  ],
  "scripts": {
    "check-version-sync": "node scripts/check-version-sync.js"
  }
}
